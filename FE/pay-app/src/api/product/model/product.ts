export interface ProductModel {
    id?: number,
    title?: string,
    amount?: string,
    buildingType?: string,
    contractType?: string,
    address?: string,
    sold?: boolean,

    order?: number
}